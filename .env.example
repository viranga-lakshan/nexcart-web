# Local dev: Vite proxies /api and /uploads to localhost:5000
VITE_API_BASE_URL=/api/v1

# Vercel (recommended): keep /api/v1 and use vercel.json rewrites to your AWS backend
# Alternative direct AWS URL (requires backend CORS for your Vercel domain):
# VITE_API_BASE_URL=http://13.60.211.231:5000/api/v1

VITE_APP_NAME=NexCart